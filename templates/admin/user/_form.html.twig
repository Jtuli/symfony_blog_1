        {{ form_start(form)}}

       
       
         <div class="row">
                    <div class="col">
                         {{ form_row(form.username)}}
                         {{ form_row(form.name)}}
                    </div>

                    <div class="col">
                         
                    </div>


                    <div class="col">
                    {% if user.profilpicture %}
                        
                        <img src="{{ vich_uploader_asset(user, 'imageFile' ) | imagine_filter('thumb') }}" alt="card-img-top" style="width:100%; height: auto;">
                    {% else %}
                        <a href="../html-link.htm">
                            <img src="{{ 'images/users/empty.jpeg' | imagine_filter('thumb') }}" alt="card-img-top" style="width:100%; height: auto;">
                        </a>

                    {% endif %}

                        <div class="mt-4">{{ form_row(form.profilpicture)}}</div>

                    </div>
        </div>

        <button class="btn btn-primary">{{ button|default('Enregistrer')}}</button>
        {{ form_end(form)}}
